export const handler = async (event) => {
  // fetch data from musicbrainz api

  // return the top relevant stuff

  https://jbmzyrrrka.execute-api.us-east-2.amazonaws.com/default/music-search?album=meow

  let param = event.queryStringParameters?.album || "none"

  if(param === "none") {
    return {
      statusCode: 200,
      body: JSON.stringify({
        message: "No album provided",
      }),
    };
  }
  else {
    return {
      statusCode: 200,
      body: JSON.stringify({
        message: `You searched for ${param}`,
      }),
    };
  }


};